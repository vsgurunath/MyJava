import java.lang.*;
import java.util.*;

public class Main {
    public static void main(String[] args) {
        // YOUR CODE GOES HERE
        // Please take input and print output to standard input/output (stdin/stdout)
        // DO NOT USE ARGUMENTS FOR INPUTS
        // E.g. 'Scanner' for input & 'System.out' for output
        Scanner scn = new Scanner(System.in);

        /*
        if (N > 0) {
            System.out.println("Positive");
        }
        if (N < 0) {
            System.out.println("Negative");
        }
        if (N == 0) {
            System.out.println("Zero");
        }
        

        int Rating = scn.nextInt(); //employee rating
        int oldSalary = scn.nextInt(); //employee current salary
        double newSalary;
        
        //Rating: 1 - no hike; 2 - no hike; 3 - 5% hike; 4 - 10% hike; 5 - 15% hike
        if (Rating == 1 || Rating == 2){
            newSalary = oldSalary;
        }  
        else if(Rating == 3){
            newSalary = oldSalary + (0.05 * oldSalary); //compiler default for decimal values: double
        }      
        else if(Rating == 4){
            newSalary = oldSalary + (0.10 * oldSalary);
        }
        else if(Rating == 5){
            newSalary = oldSalary + (0.15 * oldSalary);
        }      
        else {
            newSalary = oldSalary ;
            System.out.println("Too new to rate");
        }                   
        System.out.println("New salary:" + newSalary); 

        
        int s1 = scn.nextInt();
        int s2 = scn.nextInt();
        int s3 = scn.nextInt();
        
        if ((s1 == s2) && (s2 == s3) ){ 
            //no need to check (s1 == s3) here
            System.out.println("Equilateral triangle");
        }
        else if((s1 == s2) || (s2 == s3) || (s1 == s3)){
            System.out.println("Isosceles triangle");
        }
        //else if((s1 != s2) && (s2 != s3) && (s1 != s3)) --> this is not required
        else {
            System.out.println("Scalene triangle");
        }
        */
    }
}